const add = (a,b) => a + b
const res = (a,b) => a - b
const mul = (a,b) => a * b
const div = (a,b) => a / b
const mod = (a,b) => a % b

const calculator = {
    add, 
    res, 
    mul, 
    div, 
    mod,
}

module.exports = calculator;